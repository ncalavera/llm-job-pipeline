psycopg2-binary>=2.9
requests>=2.31
beautifulsoup4>=4.12
python-dateutil>=2.8

# Optional — only needed if you use the Firecrawl enrichment path.
firecrawl-py>=2.0

# Optional — only needed for the direct-SDK scoring path
# (`score_companies.py --api`). The normal daily flow scores through your
# coding agent's subagents (`--local`) and needs neither this package nor a key.
anthropic>=0.40

# Optional — only needed for the mail watcher (scripts/mail_watch.py):
# Gmail polling for recruiter-mail Telegram alerts.
google-api-python-client>=2.100
google-auth>=2.20
